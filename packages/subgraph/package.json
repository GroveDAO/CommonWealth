{
  "name": "@commonwealth/subgraph",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "graph build",
    "test": "graph test",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ commonwealth/commonwealth",
    "codegen": "graph codegen"
  },
  "dependencies": {
    "@graphprotocol/graph-ts": "^0.35.1"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.79.2"
  }
}
